# 🔄 Руководство по обновлению Playerok Cardinal

## 📦 Для пользователей

### Автоматическое обновление через Telegram бота

1. Откройте диалог с ботом в Telegram
2. Введите команду `/check_updates` для проверки обновлений
3. Если есть обновление, введите команду `/update`
4. Дождитесь завершения процесса обновления
5. Бот автоматически перезапустится с новой версией

### Ручное обновление

1. Скачайте последний релиз с [GitHub](https://github.com/KITUSTTT/PlayerokCardinal/releases)
2. Остановите бота (если он запущен)
3. Распакуйте архив в папку с ботом (замените файлы)
4. Запустите бота заново

⚠️ **Важно:** При обновлении сохраняются все ваши конфигурации, плагины и данные.

---

## 🛠️ Для разработчиков

### Создание нового релиза (Windows)

1. Обновите версию в файле `main.py`:
   ```python
   VERSION = "1.0.1"  # Новая версия
   ```

2. Обновите `CHANGELOG.md` с описанием изменений

3. Запустите скрипт `CREATE_RELEASE.bat`:
   ```batch
   CREATE_RELEASE.bat
   ```

4. Скрипт автоматически:
   - Создаст коммит с изменениями
   - Создаст тег с версией (например, `v1.0.1`)
   - Отправит изменения в GitHub

5. Создайте релиз на GitHub:
   - Перейдите на https://github.com/KITUSTTT/PlayerokCardinal/releases/new
   - Выберите созданный тег
   - Заполните описание (можно скопировать из `CHANGELOG.md`)
   - Нажмите "Publish release"

### Создание нового релиза (Linux)

```bash
# 1. Обновите версию в main.py
# 2. Обновите CHANGELOG.md

# 3. Создайте коммит и тег
VERSION=$(grep "VERSION = " main.py | cut -d'"' -f2)
git add .
git commit -m "Release v$VERSION"
git tag -a v$VERSION -m "Playerok Cardinal v$VERSION"

# 4. Отправьте в GitHub
git push origin main
git push origin v$VERSION

# 5. Создайте релиз на GitHub через веб-интерфейс
```

### Формат версий

Используйте семантическое версионирование:
- **MAJOR.MINOR.PATCH** (например, `1.0.0`)
- **MAJOR** - несовместимые изменения API
- **MINOR** - новая функциональность с обратной совместимостью
- **PATCH** - исправления ошибок

### Теги на GitHub

- Теги должны начинаться с `v` (например, `v1.0.0`)
- Каждый тег должен иметь соответствующий релиз на GitHub
- Описание релиза можно взять из `CHANGELOG.md`

### Проверка обновлений

Система проверки обновлений работает следующим образом:

1. Бот получает список тегов с GitHub API
2. Находит текущий тег в списке
3. Ищет следующий (более новый) тег
4. Если следующий тег найден, получает информацию о релизах
5. Показывает пользователю доступные обновления

**Важно:** 
- Текущий тег не включается в список обновлений
- Если текущий тег - последний, показывается сообщение "У тебя стоит последняя версия"

---

## 📝 Примеры

### Пример 1: Создание патч-релиза (1.0.0 → 1.0.1)

1. Исправьте ошибки в коде
2. Обновите `main.py`: `VERSION = "1.0.1"`
3. Добавьте в `CHANGELOG.md`:
   ```markdown
   ## [1.0.1] - 2025-12-07
   ### Исправлено
   - Исправлена ошибка в системе обновлений
   - Улучшена обработка прокси
   ```
4. Запустите `CREATE_RELEASE.bat`
5. Создайте релиз на GitHub

### Пример 2: Создание минорного релиза (1.0.1 → 1.1.0)

1. Добавьте новую функциональность
2. Обновите `main.py`: `VERSION = "1.1.0"`
3. Добавьте в `CHANGELOG.md`:
   ```markdown
   ## [1.1.0] - 2025-12-08
   ### Добавлено
   - Новая функция X
   - Улучшена производительность
   ### Изменено
   - Обновлен интерфейс настроек
   ```
4. Запустите `CREATE_RELEASE.bat`
5. Создайте релиз на GitHub

---

## ❓ Частые вопросы

**Q: Как откатить обновление?**
A: Скачайте предыдущий релиз с GitHub и замените файлы вручную.

**Q: Обновление не работает, что делать?**
A: 
1. Проверьте подключение к интернету
2. Убедитесь, что на GitHub есть релиз с тегом
3. Проверьте логи бота на наличие ошибок
4. Попробуйте обновиться вручную

**Q: Можно ли пропустить обновление?**
A: Да, вы можете пропустить обновление. Но рекомендуется обновляться для получения исправлений и новых функций.

**Q: Что делать, если тег уже существует?**
A: Скрипт `CREATE_RELEASE.bat` предупредит вас и предложит перезаписать тег. Или создайте новый тег с другой версией.

---

## 🔗 Полезные ссылки

- **Репозиторий:** https://github.com/KITUSTTT/PlayerokCardinal
- **Релизы:** https://github.com/KITUSTTT/PlayerokCardinal/releases
- **Создание релиза:** https://github.com/KITUSTTT/PlayerokCardinal/releases/new
- **Telegram чат:** [@kaderixfjsfsd](https://t.me/kaderixfjsfsd)

